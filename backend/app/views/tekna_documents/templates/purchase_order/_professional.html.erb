<% # Professional — Xero Standard equivalent. Clean blue accent line, logo left, details right. %>
<%= "" -%>
<style>
  .po-professional {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    font-size: 9pt;
    line-height: 1.5;
    color: #374151;
  }

  .po-professional p { margin: 2px 0; }

  /* ── Accent bar at top ── */
  .po-professional .pp-accent { height: 4px; background: #2563eb; margin: -15px -15px 0; }

  @media print {
    .po-professional .pp-accent { margin: -20mm -15mm 0; }
  }

  /* ── Header ── */
  .po-professional .pp-hdr {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    padding: 20px 0 18px;
    border-bottom: 1px solid #e5e7eb;
    margin-bottom: 20px;
  }

  .po-professional .pp-hdr-l { display: flex; gap: 12px; align-items: flex-start; }

  .po-professional .pp-logo-box {
    width: 46px;
    height: 46px;
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: 5px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18pt;
    font-weight: 700;
    color: #2563eb;
    flex-shrink: 0;
    overflow: hidden;
  }

  .po-professional .pp-logo-box img { width: 100%; height: 100%; object-fit: contain; }

  .po-professional .pp-co-name { font-size: 11pt; font-weight: 700; color: #111827; line-height: 1.2; margin-bottom: 3px; }
  .po-professional .pp-co-detail { font-size: 7.5pt; color: #9ca3af; }

  .po-professional .pp-hdr-r { text-align: right; }

  .po-professional .pp-doc-label {
    font-size: 18pt;
    font-weight: 600;
    color: #2563eb;
    line-height: 1;
    margin-bottom: 4px;
  }

  .po-professional .pp-num {
    font-size: 12pt;
    font-weight: 700;
    color: #111827;
  }

  .po-professional .pp-date { font-size: 7.5pt; color: #9ca3af; margin-top: 3px; }

  /* ── Info Panels ── */
  .po-professional .pp-info {
    display: flex;
    gap: 12px;
    margin-bottom: 20px;
  }

  .po-professional .pp-info-col {
    flex: 1;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    padding: 10px 12px;
    font-size: 8pt;
  }

  .po-professional .pp-info-hd {
    font-size: 6.5pt;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.7px;
    color: #2563eb;
    margin-bottom: 6px;
    padding-bottom: 4px;
    border-bottom: 1px solid #e2e8f0;
  }

  .po-professional .pp-info-name { font-weight: 600; color: #111827; margin-bottom: 2px; }
  .po-professional .pp-info-dim { color: #9ca3af; font-size: 7.5pt; font-style: italic; margin-top: 3px; }

  /* ── Table ── */
  .po-professional .pp-tbl {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
    font-size: 8.5pt;
    border: 1px solid #e2e8f0;
  }

  .po-professional .pp-tbl th {
    background: #2563eb;
    color: #fff;
    padding: 7px 10px;
    font-size: 7pt;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.4px;
    text-align: left;
  }

  .po-professional .pp-tbl th:nth-child(1) { width: 40px; text-align: center; }
  .po-professional .pp-tbl th:nth-child(4) { width: 80px; text-align: right; }
  .po-professional .pp-tbl th:nth-child(5) { width: 36px; text-align: center; }
  .po-professional .pp-tbl th:nth-child(6) { width: 90px; text-align: right; }

  .po-professional .pp-tbl td {
    padding: 7px 10px;
    border-bottom: 1px solid #f1f5f9;
    vertical-align: top;
    color: #374151;
  }

  .po-professional .pp-tbl td:nth-child(1) { text-align: center; color: #9ca3af; }
  .po-professional .pp-tbl td:nth-child(4) { text-align: right; }
  .po-professional .pp-tbl td:nth-child(5) { text-align: center; color: #9ca3af; }
  .po-professional .pp-tbl td:nth-child(6) { text-align: right; font-weight: 600; color: #111827; }

  .po-professional .pp-alt td { background: #f8fafc; }

  /* ── Bottom ── */
  .po-professional .pp-bottom {
    display: flex;
    gap: 20px;
    margin-bottom: 16px;
    align-items: flex-start;
  }

  .po-professional .pp-notes { flex: 1; }

  .po-professional .pp-sec-label {
    font-size: 6.5pt;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.7px;
    color: #2563eb;
    margin-bottom: 5px;
  }

  .po-professional .pp-notes-body { font-size: 8pt; color: #4b5563; line-height: 1.5; }

  .po-professional .pp-colour-tag {
    display: inline-block;
    font-size: 7pt;
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: 3px;
    padding: 1px 6px;
    margin: 2px 2px 2px 0;
    color: #1d4ed8;
  }

  .po-professional .pp-totals { width: 210px; flex-shrink: 0; }

  .po-professional .pp-tot-row {
    display: flex;
    justify-content: space-between;
    padding: 4px 0;
    font-size: 8pt;
    color: #6b7280;
    border-bottom: 1px solid #f1f5f9;
  }

  .po-professional .pp-tot-final {
    display: flex;
    justify-content: space-between;
    padding: 8px 0 4px;
    font-size: 11pt;
    font-weight: 700;
    color: #2563eb;
    border-top: 2px solid #2563eb;
    margin-top: 2px;
  }

  /* ── Footer ── */
  .po-professional .pp-remit {
    font-size: 7.5pt;
    color: #6b7280;
    padding: 10px 12px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    margin-bottom: 10px;
  }

  .po-professional .pp-warn {
    font-size: 6.5pt;
    color: #b91c1c;
    font-weight: 600;
    padding: 5px 10px;
    background: #fff8f8;
    border-left: 3px solid #dc2626;
    margin-bottom: 10px;
    line-height: 1.5;
  }

  .po-professional .pp-cond {
    font-size: 6.5pt;
    color: #94a3b8;
    border-top: 1px solid #e2e8f0;
    padding-top: 8px;
  }

  .po-professional .pp-cond ol { margin: 4px 0 0; padding-left: 14px; }
  .po-professional .pp-cond li { margin-bottom: 2px; }
</style>

<div class="po-professional">

  <!-- Blue accent bar -->
  <div class="pp-accent"></div>

  <!-- Header: logo + company left | Purchase Order + number right -->
  <div class="pp-hdr">
    <div class="pp-hdr-l">
      <div class="pp-logo-box">
        <% if company[:logo_url].present? %>
          <img src="<%= company[:logo_url] %>" alt="<%= company[:company_name] %>">
        <% else %>
          <%= company[:company_name].to_s.first.upcase %>
        <% end %>
      </div>
      <div>
        <div class="pp-co-name"><%= company[:company_name] %></div>
        <div class="pp-co-detail">ABN <%= company[:abn_formatted] %><% if company[:qbcc_license].present? %> · QBCC <%= company[:qbcc_license] %><% end %></div>
        <div class="pp-co-detail"><%= company[:phone_formatted] %><% if company[:email].present? %> · <%= company[:email] %><% end %></div>
      </div>
    </div>
    <div class="pp-hdr-r">
      <div class="pp-doc-label">Purchase Order</div>
      <div class="pp-num"><%= purchase_order[:purchase_order_number] %></div>
      <% if purchase_order[:ted_task].present? %><div class="pp-date"><%= purchase_order[:ted_task] %></div><% end %>
      <div class="pp-date">Required <%= purchase_order[:required_date] %></div>
    </div>
  </div>

  <!-- Info panels -->
  <div class="pp-info">
    <div class="pp-info-col">
      <div class="pp-info-hd">Deliver To</div>
      <div class="pp-info-name"><%= purchase_order[:delivery_address] || job[:full_address] %></div>
      <% if job[:suburb].present? %><p><%= job[:suburb] %> <%= job[:state] %> <%= job[:postcode] %></p><% end %>
      <% if job[:lot_number].present? || job[:plan_number].present? %><p class="pp-info-dim">Lot <%= job[:lot_number] %> · Plan <%= job[:plan_number] %></p><% end %>
    </div>
    <div class="pp-info-col">
      <div class="pp-info-hd">Supplier</div>
      <div class="pp-info-name"><%= purchase_order[:supplier][:name] %></div>
      <% if purchase_order[:supplier][:phone].present? %><p><%= purchase_order[:supplier][:phone] %></p><% end %>
      <% if purchase_order[:supplier][:email].present? %><p><%= purchase_order[:supplier][:email] %></p><% end %>
    </div>
    <div class="pp-info-col">
      <div class="pp-info-hd">Site Contact</div>
      <% if purchase_order[:site_supervisor][:name].present? %><div class="pp-info-name"><%= purchase_order[:site_supervisor][:name] %></div><% end %>
      <% if purchase_order[:site_supervisor][:phone].present? %><p><%= purchase_order[:site_supervisor][:phone] %></p><% end %>
      <% if purchase_order[:site_supervisor][:email].present? %><p><%= purchase_order[:site_supervisor][:email] %></p><% end %>
      <p class="pp-info-dim">Ring 3 days prior to delivery</p>
    </div>
  </div>

  <!-- Line Items -->
  <table class="pp-tbl">
    <thead>
      <tr>
        <th>Qty</th>
        <th>Description</th>
        <th>Code</th>
        <th>Rate</th>
        <th>Tax</th>
        <th>Amount</th>
      </tr>
    </thead>
    <tbody>
      <% purchase_order[:line_items].each_with_index do |item, index| %>
        <tr class="<%= index.odd? ? 'pp-alt' : '' %>">
          <td><%= item[:quantity] %></td>
          <td><%= item[:description] %></td>
          <td style="font-family:'SF Mono',Menlo,monospace;font-size:7.5pt;color:#9ca3af"><%= item[:pricebook_code] %></td>
          <td style="font-family:'SF Mono',Menlo,monospace"><%= item[:unit_price_formatted] %></td>
          <td><%= item[:gst_code] %></td>
          <td style="font-family:'SF Mono',Menlo,monospace"><%= item[:total_formatted] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <!-- Notes + Colours + Totals -->
  <div class="pp-bottom">
    <div class="pp-notes">
      <div class="pp-sec-label">Notes</div>
      <div class="pp-notes-body">
        <% if purchase_order[:special_instructions].present? %>
          <%= purchase_order[:special_instructions] %>
        <% else %>
          <span style="color:#d1d5db;font-style:italic">No special instructions</span>
        <% end %>
      </div>

      <% if defined?(colour_selections) && colour_selections[:has_selections] %>
        <% filled = colour_selections[:flat_list].select { |s| s[:colour].present? } %>
        <% if filled.any? %>
          <div class="pp-sec-label" style="margin-top:10px">Colour Selections</div>
          <div>
            <% filled.each do |s| %>
              <span class="pp-colour-tag"><strong><%= s[:item].to_s.titleize %></strong>: <%= s[:colour] %><% if s[:brand].present? %> (<%= s[:brand] %>)<% end %></span>
            <% end %>
          </div>
        <% end %>
      <% end %>
    </div>

    <div class="pp-totals">
      <div class="pp-tot-row"><span>Sub Total</span><span style="font-family:'SF Mono',Menlo,monospace"><%= purchase_order[:subtotal] %></span></div>
      <div class="pp-tot-row"><span>GST</span><span style="font-family:'SF Mono',Menlo,monospace"><%= purchase_order[:gst] %></span></div>
      <div class="pp-tot-final"><span>Total (incl GST)</span><span style="font-family:'SF Mono',Menlo,monospace"><%= purchase_order[:total] %></span></div>
    </div>
  </div>

  <!-- Remittance -->
  <div class="pp-remit">
    <div class="pp-sec-label">Remittance</div>
    Invoices to <strong><%= company[:company_name] %></strong> — <%= company[:email] %>
    <% payment_days = purchase_order[:supplier][:payment_terms_days] %>
    <% if payment_days.present? && payment_days.to_i <= 14 %>
      <div style="margin-top:2px">
        <% if payment_days.to_i <= 7 %>
          <strong>7-day account:</strong> Invoices received before COB Wednesday paid the following Thursday.
        <% else %>
          <strong>14-day account:</strong> Invoices received before COB Wednesday paid Thursday two weeks later.
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Warning -->
  <div class="pp-warn">
    The correct purchase order number and job address MUST be included on your tax invoice. Tax invoices without these details will be returned to the supplier for correction. Tax invoices exceeding the order total may delay payment.
  </div>

  <!-- Conditions -->
  <div class="pp-cond">
    <div style="font-size:6.5pt;font-weight:700;text-transform:uppercase;letter-spacing:0.7px;color:#94a3b8;margin-bottom:4px">Conditions of Acceptance</div>
    <ol>
      <li>If the purchase order is incorrect, please contact our office immediately.</li>
      <li>If additional work/items are required, then a NEW order number must be received.</li>
      <li>The correct purchase order number and job address MUST be included on your tax invoice. Tax invoices without these details will be returned to the supplier for correction.</li>
      <li>Processing of order will acknowledge acceptance of these conditions.</li>
      <li>In the instance the work is not completed by the original supplier in the specified timeframe, '<%= company[:company_name] %>' reserves the right to engage another supplier to complete the work. The new supplier will be paid from the original purchase order amount and any balance of funds will then be paid to the original supplier.</li>
      <li>Please ensure invoice is made out to <%= company[:company_name] %>.</li>
      <li>This purchase order is issued pursuant to the terms of the subcontract agreement. By accepting this purchase order you confirm that you agree to these subcontract terms.</li>
    </ol>
  </div>

</div>
