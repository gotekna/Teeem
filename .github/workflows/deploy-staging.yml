name: Deploy to Staging

# This workflow is disabled - Vercel handles frontend deployment automatically
# Backend deployment is handled by deploy-backend-staging.yml

on:
  workflow_dispatch:  # Manual trigger only if needed

jobs:
  info:
    name: Deployment Info
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Frontend: Auto-deployed by Vercel GitHub integration"
          echo "Backend: Deployed by deploy-backend-staging.yml workflow"
