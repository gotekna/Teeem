{
  "scripts": {
    "sync-agents": "cd backend && bin/rails teeem:agents:sync",
    "prepare": "husky || true"
  },
  "dependencies": {
    "dompurify": "^3.3.0",
    "isomorphic-dompurify": "^2.31.0"
  }
}
