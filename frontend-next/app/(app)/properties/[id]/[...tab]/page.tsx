/**
 * Catch-all route for property detail tabs.
 * Handles /properties/123/overview, /properties/123/tenancy, etc.
 * Re-exports the main page which parses path segments.
 */
export { default } from "../page";
