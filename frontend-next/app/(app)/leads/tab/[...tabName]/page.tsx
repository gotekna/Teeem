/**
 * Catch-all route for leads tabs
 * Handles: /leads/tab/pipeline, /leads/tab/email-proposals, etc.
 * Uses /tab/ prefix to avoid conflicts with /leads/[id] detail routes
 */
export { default } from "../../page";
